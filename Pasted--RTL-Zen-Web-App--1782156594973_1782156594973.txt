یک اپلیکیشن فارسی، RTL، موبایل‌فرست و آماده دیپلوی برای برند Zen Web App بساز.

هدف سایت:
دریافت سفارش طراحی سایت/اپلیکیشن از مشتری‌ها و هدایت آن‌ها به واتساپ و اینستاگرام.

تکنولوژی:
- React + Vite
- بدون بک‌اند
- بدون دیتابیس
- فایل‌ها سبک باشند و حجم پروژه زیر ۲۵ مگابایت بماند
- آماده GitHub، Netlify و Cloudflare باشد
- build بدون خطا اجرا شود

ساختار دیپلوی:
- خروجی build داخل dist باشد
- فایل netlify.toml در ریشه پروژه بساز
- SPA redirect فعال باشد
- هیچ وابستگی سنگین غیرضروری نصب نکن

رنگ‌بندی طبق این پالت:
- Beige: #F4F0DD
- Indigo: #A88AED
- Celery: #A6C261
- متن تیره: #24172F
- سفید: #FFFFFF

اطلاعات برند:
نام برند: Zen Web App
اینستاگرام: zen_web_app
شماره موبایل و واتساپ: 09113310449

ظاهر:
- طراحی خیلی شیک، مدرن، لطیف و زنانه
- بک‌گراند کرم روشن
- کارت‌های گرد و سایه نرم
- گرادینت بنفش و سبز ملایم
- فونت فارسی مناسب
- کاملاً ریسپانسیو مخصوص موبایل
- پایین صفحه یک نوار ناوبری موبایلی داشته باشد

بخش‌های صفحه:
1. هدر ثابت با نام Zen Web App و دکمه اینستاگرام
2. هیرو سکشن:
   - تیتر: طراحی سایت و اپلیکیشن برای کسب‌وکارهای خاص
   - متن: اگر مربی، پزشک، دندانپزشک، مجموعه اقامتی، سالن زیبایی یا صاحب برند هستید، سفارش خود را ثبت کنید تا دمو اختصاصی شما آماده شود.
   - دکمه اصلی: ثبت سفارش
   - دکمه دوم: مشاهده اینستاگرام

3. بخش پلن‌ها:
   سه کارت پلن:
   - پایه: ۶،۹۰۰،۰۰۰ تومان
   - حرفه‌ای: ۱۲،۹۰۰،۰۰۰ تومان
   - VIP: ۱۸،۹۰۰،۰۰۰ تومان
   هر کارت توضیح کوتاه داشته باشد.

4. فرم ثبت سفارش:
   فیلدها:
   - نام و نام خانوادگی
   - نام شرکت یا برند
   - شغل / حوزه فعالیت
   - شماره موبایل
   - شماره واتساپ برای ارسال نمونه
   - آدرس اینستاگرام مشتری
   - پلن مورد درخواست به صورت select/dropdown:
     پایه - ۶،۹۰۰،۰۰۰ تومان
     حرفه‌ای - ۱۲،۹۰۰،۰۰۰ تومان
     VIP - ۱۸،۹۰۰،۰۰۰ تومان
   - توضیحات تکمیلی

وقتی کاربر فرم را ارسال کرد:
- اطلاعات فرم تبدیل به متن مرتب فارسی شود
- لینک واتساپ با شماره 09113310449 باز شود
- متن سفارش داخل واتساپ آماده ارسال باشد

5. بخش نمونه مناسب برای مشتری:
   متن بنویس:
   برای هر شغل، دمو اختصاصی قابل طراحی است؛ مثل مربی ورزشی، پزشک، دندانپزشک، اقامتگاه، کافه، سالن زیبایی و فروشگاه.

6. بخش تماس:
   - دکمه تماس مستقیم
   - دکمه واتساپ
   - دکمه اینستاگرام

لینک‌ها:
واتساپ:
https://wa.me/989113310449
اینستاگرام:
https://instagram.com/zen_web_app
تماس:
tel:09113310449

فایل‌های لازم:
- package.json
- index.html
- src/main.jsx
- src/App.jsx
- src/index.css
- public/manifest.json
- public/robots.txt
- netlify.toml

netlify.toml باید این باشد:
[build]
command = "npm install && npm run build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

نکات مهم:
- از TypeScript استفاده نکن.
- vite.config.ts نساز.
- pnpm استفاده نکن.
- package-lock اگر لازم شد با npm ساخته شود.
- پروژه باید با دستور npm run build بدون خطا build شود.
- همه متن‌ها فارسی و راست‌چین باشند.
- ظاهر نهایی باید لوکس، اعتمادساز و مناسب جذب سفارش از اینستاگرام باشد.